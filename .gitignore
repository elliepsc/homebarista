# Python bytecode and caches
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
env/

# Environment variables and local secrets
.env
.env.*
!.env.example
.streamlit/secrets.toml

# Test, lint, type-check and coverage caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Build and packaging artifacts
build/
dist/
*.egg-info/
.eggs/

# Runtime outputs and local databases
*.log
logs/*
!logs/*.sample.jsonl
data/chroma_db/
# G4: exported ChromaDB snapshot contains full YouTube transcript text
# (third-party copyrighted content) — never committed to the public repo.
data/chroma_snapshot/

# Ingestion runtime state — regenerable by a fresh run, not reproducibility-critical
# (unlike data/eval_dataset.json and evals/results/*, which ARE committed — C3)
ingestion/metadata_cache.json
ingestion/progress.json

# Local notes and scratch docs
docs-notes/
streamlit_secrets.md

# Notebook checkpoints
.ipynb_checkpoints/

# IDE and OS local files
.vscode/
.idea/
.DS_Store
Thumbs.db
